package client.clientUtils;

import com.google.protobuf.ByteString;

public class Encryptor {
    
    public static ByteString encrypt(byte[] payload) {
        // encrypt

        // encryption logic
        // returns a byte[]
        
        //  convert to ByteString
        // ByteString encryptedByteString = ByteString.copyFrom(byte[] encryptedBytes)
        ByteString encryuByteString = ByteString.copyFromUtf8("encryptedBytes"); // temp

        return  encryuByteString;
    }
}
